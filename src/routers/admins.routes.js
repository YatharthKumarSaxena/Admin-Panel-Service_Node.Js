// ========== 👑 ADMIN PANEL ROUTES ==========


const express = require("express");
const router = express.Router();
const { ADMIN_BASE, ADMIN_ROUTES } = require("@configs/uri.config");

const {
  CREATE_ADMIN
} = ADMIN_ROUTES.ADMINS;

// Create Admin


module.exports = router;