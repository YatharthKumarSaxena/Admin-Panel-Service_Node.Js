const { defaultConfig } = require("./defaultTemplate");
const { adminTemplate } = require("./adminTemplate");
const { generateMasterTemplate } = require("./masterTemplate");
const { smsTemplate } = require("./smsTemplate");

module.exports = {
  defaultConfig,
  adminTemplate,
  generateMasterTemplate,
  smsTemplate
};
